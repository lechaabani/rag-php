services:
  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: rag_test
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag_secret
    ports:
      - "5432:5432"
    volumes:
      - pgvector_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rag -d rag_test"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgvector_data:
